name: My first workflow

on: push

jobs:
  first_job:
    runs_on: ubuntu-latest
    steps: 
    - name: my first step
      run: echo "hii"

    - name: list files
      run: ls
      
    - name: read files
      run: cat README.md
